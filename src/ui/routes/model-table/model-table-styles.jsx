import { makeStyles } from "@material-ui/core/styles";

export default function makeModelTableStyles() {
  return makeStyles(
    (theme) => ({
      root: {},
    }),
    { name: "ModelTable" }
  );
}
