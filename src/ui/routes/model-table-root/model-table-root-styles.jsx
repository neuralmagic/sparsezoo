import { makeStyles } from "@material-ui/core/styles";

export default function makeModelTableRootStyles() {
  return makeStyles(
    (theme) => ({
      root: {},
    }),
    { name: "ModelTableRoot" }
  );
}
